name: Rankscale → BigQuery Tag Extract

on:
  schedule:
    # Každý den v 7:30 UTC (po topic extractu v 7:00)
    - cron: "30 7 * * *"
  workflow_dispatch:
    inputs:
      backfill_start:
        description: "Od kdy stáhnout (YYYY-MM-DD, např. 2026-01-01). Prázdné = automatický denní run."
        required: false
        default: ""
      backfill_end:
        description: "Do kdy stáhnout (YYYY-MM-DD, např. 2026-03-31). Prázdné = dnes."
        required: false
        default: ""

jobs:
  extract_tags:
    name: Tag Extract
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalace závislostí
        run: pip install -r requirements.txt

      - name: Spuštění tag extractu
        env:
          RANKSCALE_API_KEY: ${{ secrets.RANKSCALE_API_KEY }}
          GCP_PROJECT:       ${{ secrets.GCP_PROJECT }}
          BQ_DATASET:        ${{ secrets.BQ_DATASET }}
          GCP_SA_JSON:       ${{ secrets.GCP_SA_JSON }}
          BACKFILL_START:    ${{ github.event.inputs.backfill_start }}
          BACKFILL_END:      ${{ github.event.inputs.backfill_end }}
        run: python src/rankscale_tag_extract.py

      - name: Oznámení při selhání
        if: failure()
        run: echo "❌ Tag extract selhal – zkontroluj logy výše"
