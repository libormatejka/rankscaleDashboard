# Cílový GCP projekt a BigQuery dataset pro Cloud Run Job "rankscale-extract".
# Uprav hodnoty níže a ulož — příkaz "gcloud run jobs create/update" je pak
# načte přes --env-vars-file=env.yaml, nemusíš nic psát do příkazové řádky.
#
# RANKSCALE_API_KEY sem NEPATŘÍ — ten jde přes Secret Manager (--set-secrets),
# viz krok 3 a 5 v README.md.

GCP_PROJECT: rankscale
BQ_DATASET: RankScaleDashboard
